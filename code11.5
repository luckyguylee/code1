#!/usr/bin/env python

def testit(func,*nkwargs,**kwargs):

	try:
		retval=func(*nkwargs,**kwargs)
		result=(True,retval)
	except Exception,diag:
		result=(Flase,str(diag))
	return result

def test():
	func=(int,long,float)
	vals=(1234,12.34,'1234','12.34')
	
	for eachFunc in funcs:
		print '_'*20
		for eachVal in vals:
			retval=testit(eachFunc,eachVal)
		
				if retval[0]：
					print '%s(%s)='%\
		(eachFunc._name_,'eachVal'),retval[1]
				else:
					print '%s(%s)=FAILED:'%\
		(eachFunc._name_,'eachVal'),retval[1]

if _name_=='_main_':
	test()
